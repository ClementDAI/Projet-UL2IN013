from obstacle import Obstacle
from salle import Salle

# def test_obstacle():
    # salle = Salle(10, 10)
    # obstacle = Obstacle(5, 5, 2, 2, 0)
    # obstacle.ajoutObstacle(salle)
    # assert len(salle.ListeObstacle) == 1
    # assert salle.ListeObstacle[0] == obstacle
    # coins = obstacle.coins()
    # assert len(coins) == 4
    # assert coins[0][0][0] < coins[0][1][0] # coin HG x < coin HD x
    # assert coins[0][0][1] > coins[0][1][1] # coin HG y > coin HD y
    # assert coins[1][0][0] < coins[1][1][0] # coin BG x < coin BD x
    # assert coins[1][0][1] < coins[1][1][1] # coin BG y < coin BD y